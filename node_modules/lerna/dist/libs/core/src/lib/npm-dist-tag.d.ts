import fetch from "npm-registry-fetch";
import { OneTimePasswordCache } from "./otplease";
interface DistTagOptions extends fetch.FetchOptions {
    defaultTag?: string;
    dryRun?: boolean;
}
/**
 * Add a dist-tag to a package.
 */
export declare function add(spec: string, tag: string | undefined, options: DistTagOptions, otpCache?: OneTimePasswordCache): Promise<void> | Promise<Record<string, unknown>>;
/**
 * Remove a dist-tag from a package.
 */
export declare function remove(spec: string, tag: string, options: DistTagOptions, otpCache?: OneTimePasswordCache): Promise<void> | Promise<Record<string, unknown>>;
/**
 * List dist-tags of a package.
 */
export declare function list(spec: string, options: DistTagOptions): Promise<void> | Promise<Record<string, unknown>>;
export {};
