export declare function makePromptVersion(resolvePrereleaseId: (existingPreId: string) => string, buildMetadata: string): (node: {
    version: string;
    name?: string;
    prereleaseId?: string;
}) => Promise<string>;
