import { ExecOptions } from "child_process";
import * as childProcess from "@lerna/child-process";
export interface GitCommitOptions {
    amend?: boolean;
    overrideMessage?: boolean;
    commitHooks?: boolean;
    signGitCommit?: boolean;
    signoffGitCommit?: boolean;
}
export declare function gitCommit(message: string, { amend, commitHooks, signGitCommit, signoffGitCommit, overrideMessage }: GitCommitOptions, opts: ExecOptions): Promise<childProcess.LernaReturnValue>;
